/*<script type="text/javascript">
      const flagTrue = "true";
      const flagFalse = "false";
      const inputs = document.querySelectorAll('.text-form');
      inputs.forEach(item => {
        const input = item.querySelecto
        r('input');
        const flag  = item.querySelector('.flag');
        input.addEventListener('change', (event) => {
          const text = event.target.textContent;
          if (input.type == "text") {
            if (!flag.classList.contains(flagTrue)) {
              flag.classList.add(flagTrue);
            } else {
              flag.classList.remove(flagTrue);
            }
          }
        });
      });
    </script>*/